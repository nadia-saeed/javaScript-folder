// Calculate and print the sum of elements in an array using a for loop.

let elements = [10, 5, 0, 5, 10];
let sum = 0;

for (let i = 0; i <elements.length; i++) {
    sum = sum+elements[i];
}

console.log(sum);
