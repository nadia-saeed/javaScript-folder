// Reverse the order of elements in an array using a while loop / for loop.

let elements = [1, 2, 5, 7, 3, 97];
for (let i = elements.length; i>=0; i--){
    console.log(elements[i]);
}
// Used a for loop.