// Calculate and print the sum of digits of a given number using a while loop.

// let n = 56
const number = 56;
const digits = number.toString().split('').map(Number);
console.log(digits); 

let sum = 0
while(n>=string.length);
sum = sum + (digits)
