// Calculate and print the average of elements in an array using a for loop.

let elements = [2 , 4 , 6 , 10.56568];

let sum = 0;
for (let i=0; i<elements.length; i++){
    sum = sum + elements[i]; 
}
    console.log(sum / elements.length); 

