// Create a function to print the given string elements (all) in uppercase.
function upperCase (str){
    let upperCaseStr = str.toUpperCase(); // toLowerCase() for small letters.
    console.log(upperCaseStr);
}
let str = 'gdubdAbdhBHS';
upperCase(str);